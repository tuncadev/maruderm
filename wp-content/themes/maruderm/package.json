{
  "name": "maruderm-theme",
  "private": true,
  "type": "module",
  "scripts": {
    "reference:sync": "node scripts/reference-assets.mjs",
    "reference:status": "node scripts/reference-handoff-status.mjs",
    "reference:check": "node scripts/reference-assets.mjs --check && node scripts/reference-handoff-status.mjs --check",
    "predev": "npm run reference:sync && npm run reference:status -- --check",
    "dev": "vite",
    "prebuild": "npm run reference:sync && npm run reference:status -- --check",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "tailwindcss": "^4.2.2",
    "vite": "^8.0.8"
  }
}
